<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Solana Wallet Tracker</title>
  <style>
    body {
      background-image: url('/images/<%= themeImage %>');
      background-size: cover;
      color: white;
      text-align: center;
      font-family: Arial, sans-serif;
    }
    .container {
      margin-top: 100px;
    }
    input {
      padding: 10px;
      width: 300px;
    }
    button {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Solana Wallet Tracker</h1>
    <form action="/balance" method="get">
      <input type="text" name="wallet" placeholder="Enter Solana Wallet Address">
      <button type="submit">Check Balance</button>
    </form>
    <% if (balance !== null) { %>
      <h2>Balance: <%= balance %> SOL</h2>
    <% } %>
    <% if (error) { %>
      <h2 style="color: red;"><%= error %></h2>
    <% } %>
  </div>
</body>
</html>
